# ExcelUtils

里面包含 Excel/CSV 文件转换与稳定 MD5 计算相关工具。

## 文档入口
- `STABLE_MD5_UTIL_GUIDE.md`：`StableMd5Util` 详细使用说明（API、规则、示例、注意事项）。
- `AGENTS.md`：AI 编码代理协作说明（架构、约定、工作流）。

## 快速命令
```bash
mvn test
mvn -DskipTests compile
mvn spring-boot:run
```
